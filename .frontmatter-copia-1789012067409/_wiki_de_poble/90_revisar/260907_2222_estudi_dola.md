# Auditoria Dola: Transició Fase 3 (B1, B2 i rols globals)

**Data:** 07/09/2026
**Veredicte General:** Fase 3 implementada parcialment però amb riscos crítics (RLS absent en taules clau i autorevisió permesa). Nota: 6.5/10.
**Estimació de temps:** ~2 setmanes (10-12 dies laborables).

## Text íntegre de la resposta:

L'estat actual és **mitjà avançat però incoherent**.

### B1 — VALIDACIÓ: `list_my_organizations`
**Veredicte:** ✅ **CORREGIT I ROBUST**
La funció ja inclou `organization.lema` tant a `returns table` com al `SELECT`. `security invoker` i `set search_path = ''` correctes.

### B2 — VALIDACIÓ: `private.ajustos` I `handle_new_user`
**Veredicte:** ⚠️ **Funcional però amb riscos**
Riscos detectats:
1. **Consentiment RGPD automàtic:** `consentiment_rgpd_at` s'omple des del metadata, cosa que no acredita una acció real. Caldrà una RPC de consentiment explícit.
2. **`tenant_id` controlable per l'usuari:** Qualsevol pot enviar un UUID al signUp i unir-se a un poble obert automàticament.
3. **Nom per defecte "Persona".**

### FASE 3 — ROLS GLOBALS I CLAIMS
**Veredicte:** ⚠️ **IMPLEMENTAT PARCIALMENT, PERÒ AMB RISCOS CRÍTICS**
Problemes crítics detectats al codi real de `schema.sql`:
1. **`user_platform_roles` a `public` i sense RLS:** 🔴 ALTA. Una taula de rols globals ha d'estar a `private` o tindre RLS estricte.
2. **Falta RLS a `organization_claims`:** 🔴 ALTA. La taula és pública i desprotegida.
3. **`resol_reclamacio` permet autorevisió:** 🔴 ALTA. Un superadmin pot aprovar la seua pròpia reclamació. Falta comprovació `v_claim.user_id <> auth.uid()`.
4. **Només es pot reclamar si no hi ha owner:** ⚠️ Mitjà. (Coincideix amb Claude en el bloqueig).
5. **Falta taula d'auditoria i RPC de transferència.**

### NOTA DE COMPLEXITAT I ESTIMACIÓ
**Score: 6.5 / 10**
**Estimació:** 10-12 dies laborables (≈ 2 setmanes).

**Prioritats:**
1. Moure `user_platform_roles` a `private` o afegir RLS.
2. Afegir RLS a `organization_claims` i evitar autorevisió a `resol_reclamacio`.
3. Funció `accept_rgpd()` explícita.
