---
tipus: estudi_auditoria
estat: obert
description: "Estudi de les propostes de Dola per a l'auditoria de NotesEditor i UniversalCard"
---

# 🕵️ ESTUDI D'AUDITORIA: DOLA (260904_0150)

## 1. Entrada Rebutjada / Proposada
Dola ha fet un anàlisi molt precís de les inconsistències atòmiques entre components:
1. **Píndoles (`NotesContext.jsx`)**: Confirma el diagnòstic de Grok sobre el *hardcode* de la categoria "Mur" i l'ús de classes incorrectes com `sdp-badge-neutral`. Proposa la seua neteja.
2. **Hero Image (`NotesEditor.jsx`)**: Torna a diagnosticar l'AI slop i els estils inline dels botons. (Nota: Aquest codi ja l'hem netejat en resposta a l'auditoria prèvia de Gemini, però Dola estava llegint el bundle original).
3. **Inconsistència Atòmica (`UniversalComponents.jsx`)**: Ha caçat una perla d'or. La `UniversalPage` suporta `onClick` en les píndoles (per permetre la filtració tàctil), però la `UniversalCard` ho ignora completament al seu `.map`. Si les targetes són la base del Mur, les seues etiquetes han de ser clicables.

## 2. Anàlisi DAFO de la Proposta

### Debilitats (D)
- Ha perdut temps analitzant els estils inline de `NotesEditor.jsx` que ja havíem netejat nosaltres, però això és per culpa de la naturalesa asíncrona de les auditories (llegia la mateixa versió de codi).

### Amenaces (A)
- Si afegim `onClick` a la `UniversalCard` sense estar segurs de com es gestiona la propagació de l'event (el famós `e.stopPropagation()`), fer clic a l'etiqueta podria obrir la targeta sencera per accident. S'ha de fer amb atenció.

### Fortaleses (F)
- Identificació mil·limètrica de la inconsistència del `onClick` entre targeta i pàgina. Ha anat al detall del codi base, demostrant que entén el contracte de la SKILL.

### Oportunitats (O)
- Aprofitar la refactorització de React (l'operació a cor obert de Kimi) per incloure aquesta xicoteta però vital modificació a `UniversalComponents.jsx`.

## 3. Matriu d'Importància i Urgència (Eisenhower)
- **Important i Urgent:** Aplicar el suport de `onClick` a les píndoles de la `UniversalCard`.
- **(Ja resolt):** Neteja del codi inline de `NotesEditor.jsx`.

## 4. Pla d'Acció Fusionat Definitiu (Grok + Kimi + Dola)
El llistat de tasques quirúrgiques a realitzar creix amb una nova missió:
1. **Context d'Acer (Kimi+Grok):** `useCallback` massiu, extracció de la funció pura `buildNoteLabels`, eliminar hardcode "Mur", protecció de `localStorage`.
2. **Editor a prova de bales (Kimi):** Sanejament de timeouts a l'autosave, confirmació d'esborrat, `beforeunload` segur.
3. **Sidebar Ocupat (Kimi):** Llevar `loadNote` redundant.
4. **UniversalPage i UniversalCard (Kimi+Dola):** Posar la `key` a les píndoles de la Page i donar suport `onClick` i accessibilitat a les píndoles de la Card, assegurant el `stopPropagation`.

---
**Peticions al Mestre:**
Tenim els estudis tancats. El diagnòstic d'arquitectura està completament destil·lat entre les visions de Grok, Kimi i Dola, ignorant la palla i agafant l'or. Si dones la llum verda, procedisc a aplicar la Gran Refactorització seguint el pla definitiu.
